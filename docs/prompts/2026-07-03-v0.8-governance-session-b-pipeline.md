# Claude Code Session B — rag-fish/noesisnoema-pipeline (feature branch + PR, main protected)

Session rule: 1 prompt = 1 repo = 1 CLI session. Start with `git fetch && git pull`, branch from main: `feat/v0.8-governance`.

## Task
Implement pipeline-side v0.8: RAGpack manifest v1.3, G3 promotion gate, audit chain, evidence packages. Specs: `spec-g3-promotion-gate.md`, `spec-audit-pipeline.md`, schema `ragpack-manifest-v1.3.schema.json` (provided alongside).

## Deliverables
1. **Manifest v1.3**
   - `schemas/ragpack-manifest-1.3.schema.json` (verbatim from provided file)
   - Validator module + integration into pack build: builder now emits v1.3 manifests (provenance from source registry, integrity hashes). v1.2 read path untouched (additive compatibility).
2. **G3 gate CLI** (`noema-gate`: run / stamp / verify) per spec §CLI, reusing the existing retrieval harness — do not re-implement embedding/mean-centering; import the code path used by pack QA. If no shared harness exists, extract one first (small refactor PR-internal, flag it in the PR description).
3. **Audit modules** (`noema_audit/`, `noema_evidence/`) per spec-audit-pipeline.md §5, including:
   - RFC 8785 JCS canonicalizer (numbers-as-strings rule enforced at emitter level: reject any float in event payloads)
   - `audit/conformance/vectors.json` — generate ≥10 vectors and check them into the repo; these are the cross-language contract for the future Swift emitter
   - Ed25519 via `cryptography` package
4. **Tests** (pytest): threshold boundary, hash mismatch refusal, dangling gold-set ids, policy sha mismatch, re-stamp refusal, chain break detection, signature verify, content-leak assertion (grep events for any string >64 chars that is not hex/base64), conformance vectors.
5. Archive this prompt to the prompts archive per repo convention.

## Constraints
- Python 3.11, existing project tooling/lint config respected.
- English comments. No new heavy dependencies beyond `cryptography`, `jsonschema`, `pyyaml` (all likely present; check first).
- PR title: `feat: v0.8 governance — manifest v1.3, G3 gate, audit chain (Noema ADR-0003)`. PR body links rag-fish/RAGfish docs/noema/adr/ADR-0003-noema-governance-platform.md and lists the five failure-mode tests explicitly.

## Done means
- CI green, PR open against main (do NOT merge; Taka + Max review).
- `noema-gate run` demonstrated in PR description against a synthetic 30-query gold-set fixture.

---

## Session notes (recorded during execution)

- Spec files (`spec-g3-promotion-gate.md`, `spec-audit-pipeline.md`,
  `ragpack-manifest-v1.3.schema.json`) were not attached directly to the
  prompt; located under `~/Downloads/noema-v0.8/` (also mirrored under
  `~/Downloads/files20260702/`) and used verbatim (byte-identical across both
  copies).
- Work was done in the pre-existing git worktree at
  `../noesisnoema-pipeline-v0.8-governance` (branch `feat/v0.8-governance`,
  already up to date with `origin/main`, clean working tree) rather than the
  primary checkout, which had unrelated uncommitted staged work
  (`quality/corpus_quality.py`) from a different session that was left
  untouched.
- No shared retrieval harness existed prior to this PR — mean-centering +
  cosine top-k lived only inline in the notebook UAT cell
  (`notebooks/build_ragpack_v1_2.ipynb`, cell 28). Extracted into
  `retrieval/harness.py` plus a new `LlamaCppEmbedder.embed_query` method
  (`"search_query: "` task-prefix counterpart to the existing
  `embed_texts`/`"search_document: "` path); `noema_gate` imports this
  harness rather than re-deriving the geometry.
- `noema-gate stamp` accepts an additional, spec-CLI-table-exceeding
  `--policy` option (optional): it is the only way to detect "policy edited
  between run and stamp" (failure mode 4) without an out-of-band lookup the
  spec does not define.
- Manifest v1.3 is emitted opt-in via a new `--manifest-version {1.2,1.3}`
  CLI flag on `nn-pipeline build` (default remains `1.2`), rather than
  flipping the CLI's default output version — the additive-compatibility
  constraint was read as "v1.2 stays fully supported," not "v1.3 must be the
  default," to avoid any risk of silently changing existing consumers'
  output.
- No pytest-driving CI workflow exists in this repo yet (only Discord
  push/release notification workflows) — "CI green" is trivially true; all
  tests were run locally instead (see PR description for counts).
