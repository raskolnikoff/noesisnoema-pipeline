# Requirements for noesisnoema-pipeline chunker module
# Core dependencies for token-based chunking
transformers>=4.20.0
torch>=1.9.0

# For running tests
pytest>=6.0.0

# For notebook functionality (optional, only needed for Colab)
sentence-transformers>=2.0.0
PyPDF2>=2.10.0
numpy>=1.21.0
ipywidgets>=7.6.0