# noesisnoema-pipeline
# Deterministic RAGPack Generator

# Token-based chunking
transformers>=4.20.0

# PDF text extraction (layout-aware; preserves inter-word whitespace).
# pymupdf won a bake-off against pypdf (default + layout) and pdfplumber on the
# real source PDF; pypdf's default mode produced whitespace-stripped token-soup
# that broke retrieval (PR #21 audit / docs/audit). Used by the notebook
# PDF->txt step and importable via `extraction.extract_pdf_text`.
pymupdf>=1.24.0

# llama.cpp GGUF embedding generation (RAGpack v1.2 / ADR-0011 §5)
# nomic-embed-text-v1.5 embedder; produces the GGUF file-hash identity the
# NoesisNoema app validates on import.
llama-cpp-python>=0.3.0

# Deterministic embedding generation (EPIC3 Stage 2)
# DEPRECATED: produces v1.1 manifests, not compatible with NoesisNoema v0.4+.
# Retained until DeterministicEmbedder is removed in a follow-up cleanup PR.
sentence-transformers>=3.0.0,<4.0.0

# Numerical utilities
numpy>=1.21.0

# Schema validation (manifest v1.2 conformance tests)
jsonschema>=4.0.0

# Parquet serialisation for chunks.parquet (EPIC3 Stage 3)
pyarrow>=14.0.0

# CLI entrypoint (EPIC3 Stage 3)
typer>=0.9.0

# G3 policy manifest parsing (noema-gate; Noema ADR-0003 / v0.8)
pyyaml>=6.0

# Ed25519 hash-chain signing / evidence package verification (Noema v0.8)
cryptography>=41.0.0

# Testing
pytest>=6.0.0

